import React from 'react'
import { expect } from 'chai'
import App from '../app'
import { mount } from 'enzyme'

describe('App', () => {
    it('It exists!', () => {
    	expect(Component).to.exist
    })
})
